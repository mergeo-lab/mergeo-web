import { API_BASE } from '../api/config';

const BASE = `${API_BASE}/product`;
export const PRODUCT = `${BASE}`;
export const PROVIDER_PRODUCT_SEARCH = `${BASE}/search`;
export const PROVIDER_NEW_PRODUCT_SEARCH = `${BASE}/search/newProducts`;
export const PRODUCT_LISTS = `${BASE}/lists`;
export const PRODUCT_ADD_MULTIPLE = `${BASE}/add`;
export const PRODUCT_METADATA = `${BASE}/metadata`;
export const PRODUCT_UPLOAD = `${BASE}/upload`;
export const PRODUCT_FAVORITE = `${BASE}/favorite`;
export const PRODUCT_BLACKLIST = `${BASE}/blacklist`;
export const PRODUCT_PRESENTATIONS = `${BASE}/net-contents`;
